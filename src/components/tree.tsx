import * as React from 'react'
import { StatelessComponent } from 'react'
import { hierarchy } from 'd3-hierarchy'

const Tree: StatelessComponent<{}> = (props) => {
    return (
        <g>
            
        </g>
    )
}
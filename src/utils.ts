import * as React from 'react'
import * as ReactDom from 'react-dom'


// 获取虚拟节点渲染后的 box 大小
// 构建一个虚拟节点
// export function getSvgBBox(ele: React.ReactElement): ISVGPropsRect {
    

    // fragment.appendChild()

    // const svg = document.createElement('svg')

    // svg.appendChild('')

    // fragment.appendChild(svg)

    // const virtualDom = document.createElement('svg')
    // document.documentElement.appendChild(virtualDom)
    // console.log(virtualDom)  
    // const aa = ReactDom.render(</>, fragment)

    // console.log(ele)
    // 构建一个虚拟的节点
    // 

    // return null
    // return {
    //     x: 0,
    //     y: 0,
    //     width: 0,
    //     height: 0,
    //     bottom: 0,
    //     left: 0,
    //     right: 0,
    //     top: 0
    // }
    //return { x: 0, y: 0, width: 0, height: 0 }
// }